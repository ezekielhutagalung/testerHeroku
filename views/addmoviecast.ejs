<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ADD CAST</title>
</head>
<body>
    <h1>🦞 🦞 </h1>
    <h1>  Add Cast at movie  </h1>
    <h2><%= movie.name%></h2>
    <%- include('partial/navbar') %>
    <br>
    <form action="/movies/<%= movie.id%>/addcast", method="POST">
      <div class="form-group">
          <label for="CastId">Actor</label>
          <select class="custom-select" id="CastId" name="CastId">

            <% casts.forEach(el=>{ %>
                <option value="<%=el.id%>"><%= el.getFullName()%></option>
            <%})%>
          </select>
      </div>
      <br>
      <div class="form-group">
        <label for="role">Role</label>
       <input type="text" class="form-group" id="role" name="role" placeholder="e.g pemeran utama">
       <br>
       <br>
      <button type="submit" class="btn btn-primary">Save</button>
      <button type="submit" class="btn btn-danger" onclick="history.back()">Cancel</button>
      <br>
      <br>
    </form> 
    <table border="4">
         <tr>
               <th>Actor</th>
               <th>Role</th>

         </tr>         
         
            <% for( let i = 0; i < movie.Casts.length; i++){ %>
                <tr>
                    <td><%= movie.Casts[i].first_name %> <%= movie.Casts[i].last_name %> </td>
                    <td><%= movie.Casts[i].dataValues.MovieCast.dataValues.role %></td>
                </tr>   
            <% } %>

        



    </table>
    
</body>
</html>